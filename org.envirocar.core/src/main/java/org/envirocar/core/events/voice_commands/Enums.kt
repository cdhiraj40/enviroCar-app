package org.envirocar.core.events.voice_commands

class Enums {
    enum class Recording {
        START,
        STOP,
        CHANGE_VIEW,
        DISTANCE,
        TRAVEL_TIME,
        TIME
    }
}